name: (disabled) Legacy Deploy Workflow

# Este workflow quedó obsoleto y fue reemplazado por deploy-workflow.yml.
# Lo mantenemos sólo para referencia y ejecución manual si fuera necesario.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy deploy workflow is disabled. Use .github/workflows/deploy-workflow.yml"
