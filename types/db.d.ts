// Project-local low-level SQL client type placeholder.
// This intentionally uses `any` as a temporary migration shim. Replace
// with a stricter definition (or import the Drizzle `Sql` type) when the
// rest of the codebase has been migrated to the concrete contract.
export type LowLevelSql = any;
