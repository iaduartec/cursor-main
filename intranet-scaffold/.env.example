# Ejemplo de variables de entorno
# Prefer POSTGRES_URL / DATABASE_URL for Neon/Postgres deployments
POSTGRES_URL=postgresql://user:password@db.host:5432/postgres
# Supabase-specific variables were removed from this example. If you still
# rely on the Supabase JS SDK for auth/storage keep SUPABASE_URL and
# SUPABASE_SERVICE_ROLE_KEY in your real environment, but prefer POSTGRES_URL
# for DB connections. Example values intentionally omitted.
ENABLE_DB_IN_DEV=1
